[package]
name = "oxionnx-ops"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
documentation.workspace = true
homepage.workspace = true
rust-version.workspace = true
readme = "README.md"
description = "Operator implementations for OxiONNX — 88+ ONNX operators in Pure Rust"
keywords = ["onnx", "operators", "inference", "neural-network", "pure-rust"]
categories = ["science", "mathematics"]

[dependencies]
oxionnx-core = { workspace = true }
oxifft = { workspace = true }
matrixmultiply = { workspace = true }
half = { workspace = true }

[dev-dependencies]

[features]
default = []
simd = []

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
rayon = { workspace = true }
